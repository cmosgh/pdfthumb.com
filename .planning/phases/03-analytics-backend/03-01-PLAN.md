---
phase: 03-analytics-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pdfthumbnailpro-be/src/analytics/entities/usage-event.entity.ts
  - pdfthumbnailpro-be/src/migrations/<timestamp>-AddUsageEventsTable.ts
autonomous: true
requirements: [ANLX-01]

must_haves:
  truths:
    - "A `usage_events` table exists in the database with columns: id, userId, apiKeyId, statusCode, success, errorCode, durationMs, createdAt"
    - "The `UsageEvent` TypeORM entity is defined with the correct column types (timestamp in prod, datetime in dev/test) matching the established codebase pattern"
    - "Indexes on `(userId, createdAt)` and `(createdAt)` exist to support per-user and platform-wide time-range queries"
    - "The migration runs cleanly against Postgres (production) and the entity auto-syncs in SQLite (dev/test)"
  artifacts:
    - path: "pdfthumbnailpro-be/src/analytics/entities/usage-event.entity.ts"
      provides: "TypeORM entity for usage_events table"
      exports: ["UsageEvent", "ErrorCode"]
      contains: "Entity('usage_events')"
    - path: "pdfthumbnailpro-be/src/migrations/<timestamp>-AddUsageEventsTable.ts"
      provides: "Production Postgres migration for usage_events table"
      contains: "CREATE TABLE \"usage_events\""
  key_links:
    - from: "pdfthumbnailpro-be/src/analytics/entities/usage-event.entity.ts"
      to: "TypeORM entity scanning glob (entities: [__dirname + '/**/*.entity{.ts,.js}'])"
      via: "app.module.ts TypeOrmModule.forRootAsync entities glob"
      pattern: "\\.entity\\."
    - from: "pdfthumbnailpro-be/src/migrations/<timestamp>-AddUsageEventsTable.ts"
      to: "migration runner"
      via: "npm run migration:run (production only)"
      pattern: "MigrationInterface"
---

<objective>
Create the `UsageEvent` TypeORM entity and the Postgres migration for the `usage_events` table.

Purpose: Establish the database schema that Phase 3 analytics tracking depends on. The entity drives SQLite auto-sync in dev/test. The migration runs in production. Both must exist before any processor or service can persist or query analytics data.
Output: `usage-event.entity.ts` with full column and index definitions, and a timestamped migration file following the established codebase migration naming convention.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analytics-backend/03-CONTEXT.md
@.planning/phases/03-analytics-backend/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UsageEvent entity</name>
  <files>pdfthumbnailpro-be/src/analytics/entities/usage-event.entity.ts</files>
  <action>
    Create the directory `pdfthumbnailpro-be/src/analytics/entities/` and the entity file.

    The entity must follow the established codebase pattern from `api-key.entity.ts`:
    - `const isProduction = process.env.NODE_ENV === 'production';` at module level (not inside class)
    - `type: isProduction ? 'timestamp' : 'datetime'` for date columns

    Export the `ErrorCode` enum:
    ```typescript
    export enum ErrorCode {
      INVALID_PDF = 'INVALID_PDF',
      TIMEOUT = 'TIMEOUT',
      UNKNOWN = 'UNKNOWN',
    }
    ```

    Entity columns (all per CONTEXT.md — do NOT add `eventType`):
    - `id`: `@PrimaryGeneratedColumn('uuid')`
    - `userId`: `@Column({ type: 'varchar', nullable: true })` — null for unauthenticated (unlikely but safe)
    - `apiKeyId`: `@Column({ type: 'varchar', nullable: true })` — null for JWT-auth requests; resolved by processor
    - `statusCode`: `@Column({ type: 'int', nullable: false })`
    - `success`: `@Column({ type: 'boolean', nullable: false })`
    - `errorCode`: `@Column({ type: 'varchar', nullable: true })` — stores ErrorCode enum value as VARCHAR string (not a Postgres enum type — easier to extend without migrations)
    - `durationMs`: `@Column({ type: 'int', nullable: false })`
    - `createdAt`: `@Column({ type: isProduction ? 'timestamp' : 'datetime', default: () => 'CURRENT_TIMESTAMP' })`

    Decorators:
    - `@Entity('usage_events')` on the class
    - `@Index(['userId', 'createdAt'])` — supports per-user time-range queries
    - `@Index(['createdAt'])` — supports platform-wide time-range queries

    Imports from `typeorm`: `Entity`, `PrimaryGeneratedColumn`, `Column`, `Index`

    No relations (no `@ManyToOne` to User or ApiKey) — keeps the entity simple and avoids FK constraint issues when userId is null. The analytics queries join via raw SQL when needed.
  </action>
  <verify>
    Run `npx tsc --noEmit` in `pdfthumbnailpro-be/` — zero new TypeScript errors.
    Confirm file exists at `src/analytics/entities/usage-event.entity.ts`.
    Confirm `ErrorCode` enum and `UsageEvent` class are both exported.
  </verify>
  <done>
    `usage-event.entity.ts` exists, exports `UsageEvent` and `ErrorCode`, uses the isProduction timestamp branch, has two @Index decorators. `npx tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AddUsageEventsTable migration</name>
  <files>pdfthumbnailpro-be/src/migrations/<timestamp>-AddUsageEventsTable.ts</files>
  <action>
    Generate a timestamp using `Date.now()` (same approach as existing migrations — e.g., `1762960360841`) and create the migration file: `src/migrations/<timestamp>-AddUsageEventsTable.ts`.

    Follow the exact migration class naming pattern from existing migrations:
    - Class name: `AddUsageEventsTable<timestamp>` implementing `MigrationInterface`
    - `name` property: `'AddUsageEventsTable<timestamp>'`

    The `up()` method must use `queryRunner.query()` with raw Postgres SQL (production only — dev/test uses `synchronize: true`):

    ```sql
    CREATE TABLE "usage_events" (
      "id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      "userId" varchar,
      "apiKeyId" varchar,
      "statusCode" integer NOT NULL,
      "success" boolean NOT NULL,
      "errorCode" varchar,
      "durationMs" integer NOT NULL,
      "createdAt" timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
    )
    ```

    Then add the two indexes:
    ```sql
    CREATE INDEX "IDX_usage_events_userId_createdAt" ON "usage_events" ("userId", "createdAt")
    ```
    ```sql
    CREATE INDEX "IDX_usage_events_createdAt" ON "usage_events" ("createdAt")
    ```

    The `down()` method must drop indexes then the table:
    ```sql
    DROP INDEX IF EXISTS "IDX_usage_events_userId_createdAt"
    DROP INDEX IF EXISTS "IDX_usage_events_createdAt"
    DROP TABLE IF EXISTS "usage_events"
    ```

    IMPORTANT: Do NOT add foreign key constraints from `userId` or `apiKeyId` to `users` or `api_keys`. The entity has no relations; the table stores string IDs only. This avoids FK constraint issues on delete cascades and keeps analytics data intact even if a user/key is deleted.
  </action>
  <verify>
    File exists at `src/migrations/<timestamp>-AddUsageEventsTable.ts`.
    `npx tsc --noEmit` in `pdfthumbnailpro-be/` passes.
    Class implements `MigrationInterface` with both `up()` and `down()` methods.
    `up()` creates the table and both indexes. `down()` drops them.
  </verify>
  <done>
    Migration file exists with correct naming convention, implements MigrationInterface, creates `usage_events` table with all 8 columns and 2 indexes in `up()`, drops them in `down()`. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- `pdfthumbnailpro-be/src/analytics/entities/usage-event.entity.ts` exists and exports `UsageEvent` + `ErrorCode`
- Migration file exists in `pdfthumbnailpro-be/src/migrations/` with `AddUsageEventsTable` in the name
- `npx tsc --noEmit` in `pdfthumbnailpro-be/` produces zero new errors
- Entity column types use the `isProduction ? 'timestamp' : 'datetime'` branch (matching `api-key.entity.ts` pattern)
- Entity has `@Index(['userId', 'createdAt'])` and `@Index(['createdAt'])` decorators
- Entity is discoverable by TypeORM's glob `entities: [__dirname + '/**/*.entity{.ts,.js}']` — no manual registration needed yet
</verification>

<success_criteria>
- `UsageEvent` entity file exists with all 8 columns, `ErrorCode` enum, and 2 index decorators
- Migration file exists with correct class name + timestamp pattern, creates table + indexes in `up()`, drops in `down()`
- Zero TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-backend/03-01-SUMMARY.md`
</output>
